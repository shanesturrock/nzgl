#!/bin/bash

if [[ -p /dev/stdin ]]; then
  # echo "stdin is from a pipe"
  cat - | ssh 10.0.2.102 bsub

elif [[ ! -t 0 && ! -p /dev/stdin ]]; then
  # echo "improved read from redirection"
  cat - | ssh 10.0.2.102 bsub

else
  # echo "current style"
  echo bsub $* | ssh -T 10.0.2.102
fi
